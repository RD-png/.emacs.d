Do we want to apply the changes automatically after updates to arguments and filters ?

Or do we want to apply the filters automatically but arguments only when the user clicks the apply filters button
